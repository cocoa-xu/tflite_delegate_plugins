name: build

# Called by ci.yml and release.yml so the matrix is written once. It has no
# triggers of its own on purpose: what starts it is the caller's business.
on:
  workflow_call:

env:
  # LITERT_VER is what a plugin has to match: the runtime is built from LiteRT's
  # tflite subtree. TFLITE_VER is the TensorFlow release LiteRT's own build
  # reaches into for compiler/mlir/lite, TSL and XLA.
  LITERT_VER: "2.2.0"
  TFLITE_VER: "2.21.0-rc0"

jobs:
  macos:
    runs-on: ${{ matrix.runs_on }}
    name: ${{ matrix.triple }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { runs_on: macos-14, triple: aarch64-apple-darwin }
          - { runs_on: macos-15-intel, triple: x86_64-apple-darwin }

    steps:
      - uses: actions/checkout@v7

      - name: Cache TensorFlow sources
        uses: actions/cache@v6
        with:
          path: build/_deps/tensorflow-src
          key: tf-src-${{ env.TFLITE_VER }}

      - name: Cache LiteRT sources
        uses: actions/cache@v6
        with:
          path: build/_deps/litert-src
          key: litert-src-${{ env.LITERT_VER }}

      - name: Install ccache
        run: brew install ccache

      - name: Restore ccache
        id: ccache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-${{ matrix.triple }}-${{ env.LITERT_VER }}-${{ hashFiles('CMakeLists.txt') }}-${{ github.run_id }}
          restore-keys: |
            ccache-${{ matrix.triple }}-${{ env.LITERT_VER }}-${{ hashFiles('CMakeLists.txt') }}-
            ccache-${{ matrix.triple }}-${{ env.TFLITE_VER }}-
            ccache-${{ matrix.triple }}-

      - name: Configure ccache
        run: |
          # Through the environment rather than --set-config, which writes a file
          # under whichever directory ccache resolves at the time and that the
          # build then never reads. The path is explicit rather than ~/.cache:
          # the Linux jobs run inside a container where HOME is not the runner's,
          # and a cache step aimed at the wrong directory saves an empty one while
          # reporting nothing wrong.
          D="${{ github.workspace }}/.ccache"
          echo "CCACHE_DIR=$D" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=400M" >> $GITHUB_ENV
          # Whether the restore step actually put anything here. Asking the cache
          # action is not an option: its key carries the run id so it never
          # matches exactly, and the output that would say what the restore-keys
          # matched belongs to actions/cache/restore, not to actions/cache.
          if [ -d "$D" ] && [ -n "$(ls -A "$D" 2>/dev/null)" ]; then
            echo "CCACHE_RESTORED=1" >> $GITHUB_ENV
          fi
          # $GITHUB_ENV reaches the next step, not this one, so the directory has
          # to be named again here or the counters zeroed are the default ones.
          CCACHE_DIR="$D" ccache --zero-stats

      - name: Build
        run: |
          cmake -S . -B build -D CMAKE_BUILD_TYPE=RelWithDebInfo -D LITERT_VER="${LITERT_VER}" -D TFLITE_VER="${TFLITE_VER}" -D TFLITE_PLUGINS_COREML=OFF -D CMAKE_C_COMPILER_LAUNCHER=ccache -D CMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build -j$(sysctl -n hw.ncpu)

      - name: Verify
        run: |
          cc -o build/load_plugin test/load_plugin.c
          for lib in build/lib/*.dylib; do
            ./scripts/verify_plugin.sh "$lib"
            ./build/load_plugin "$lib" --reject-unknown-options
          done

      - name: Package
        run: |
          mkdir -p dist/lib debug
          cp build/lib/*.dylib dist/lib/
          # Debug info lives outside the binary on Mach-O: dsymutil collects it
          # into a bundle, then the .dylib is stripped of everything but the two
          # exported entry points.
          for lib in dist/lib/*.dylib; do
            dsymutil "$lib" -o "debug/$(basename "$lib").dSYM"
            strip -x "$lib"
          done
          ls -l dist/lib debug

          PKG="tflite_delegate_plugins-${{ matrix.triple }}.tar.gz"
          DBG="tflite_delegate_plugins-${{ matrix.triple }}-debug.tar.gz"
          tar -C dist -czf "$PKG" lib
          tar -C debug -czf "$DBG" .
          shasum -a 256 "$PKG" | tee "$PKG.sha256"
          shasum -a 256 "$DBG" | tee "$DBG.sha256"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.triple }}
          path: |
            *.tar.gz
            *.tar.gz.sha256

      - name: ccache statistics
        if: always()
        run: |
          ccache --show-stats | head -20
          # A restored cache with no hits means the path or the key is wrong, and
          # the only symptom is a job quietly as slow as a cold one.
          if [ -n "${CCACHE_RESTORED:-}" ]; then
            # ccache 4 prints "Hits:". ccache 3, which is what ubuntu:20.04
            # carries, prints "cache hit (direct)" and "(preprocessed)".
            # Reading only the former counts every ccache 3 run as zero hits and
            # fails a job whose cache was working fine.
            stats=$(ccache --show-stats)
            hits=$(printf '%s' "$stats" | awk '/^  Hits:/{print $2; exit}')
            if [ -z "$hits" ]; then
              direct=$(printf '%s' "$stats" | awk '/^cache hit \(direct\)/{print $NF; exit}')
              prep=$(printf '%s' "$stats" | awk '/^cache hit \(preprocessed\)/{print $NF; exit}')
              hits=$(( ${direct:-0} + ${prep:-0} ))
            fi
            if [ "${hits:-0}" -eq 0 ]; then
              echo "::error::a ccache cache was restored but nothing hit; check CCACHE_DIR against the cached path"
              exit 1
            fi
          fi


  linux:
    # Native on both arches: GitHub's arm64 runners removed the need to
    # cross-compile, and a cross build would need a host flatc that TFLite
    # demands through TFLITE_HOST_TOOLS_DIR. The ubuntu:20.04 container keeps
    # glibc at 2.31 so the artifacts run on older targets than the runner.
    runs-on: ${{ matrix.runs_on }}
    container: ubuntu:20.04
    name: ${{ matrix.triple }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { arch: x86_64, triple: x86_64-linux-gnu, runs_on: ubuntu-latest }
          - { arch: aarch64, triple: aarch64-linux-gnu, runs_on: ubuntu-24.04-arm }

    steps:
      - name: Install system dependencies
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update -qq
          # gcc-10 as well as build-essential: focal's default is 9, and the
          # abseil LiteRT pins refuses anything below 10. Staying on focal is
          # what keeps glibc at 2.31, so the compiler moves and the sysroot does
          # not.
          apt-get install -y -qq build-essential gcc-10 g++-10 curl git unzip ca-certificates python3 ninja-build binutils ccache
          curl -fsSL "https://github.com/Kitware/CMake/releases/download/v3.28.6/cmake-3.28.6-linux-$(uname -m).tar.gz" -o /tmp/cmake.tar.gz
          tar -xzf /tmp/cmake.tar.gz --strip-components=1 -C /usr/local

      - uses: actions/checkout@v7

      - name: Cache TensorFlow sources
        uses: actions/cache@v6
        with:
          path: build/_deps/tensorflow-src
          key: tf-src-${{ env.TFLITE_VER }}

      - name: Cache LiteRT sources
        uses: actions/cache@v6
        with:
          path: build/_deps/litert-src
          key: litert-src-${{ env.LITERT_VER }}-${{ matrix.arch }}

      - name: Restore ccache
        id: ccache
        uses: actions/cache@v6
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-${{ matrix.triple }}-${{ env.LITERT_VER }}-${{ hashFiles('CMakeLists.txt') }}-${{ github.run_id }}
          restore-keys: |
            ccache-${{ matrix.triple }}-${{ env.LITERT_VER }}-${{ hashFiles('CMakeLists.txt') }}-
            ccache-${{ matrix.triple }}-${{ env.TFLITE_VER }}-
            ccache-${{ matrix.triple }}-

      - name: Configure ccache
        run: |
          # Through the environment rather than --set-config, which writes a file
          # under whichever directory ccache resolves at the time and that the
          # build then never reads. The path is explicit rather than ~/.cache:
          # the Linux jobs run inside a container where HOME is not the runner's,
          # and a cache step aimed at the wrong directory saves an empty one while
          # reporting nothing wrong.
          D="${{ github.workspace }}/.ccache"
          echo "CCACHE_DIR=$D" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=400M" >> $GITHUB_ENV
          # Whether the restore step actually put anything here. Asking the cache
          # action is not an option: its key carries the run id so it never
          # matches exactly, and the output that would say what the restore-keys
          # matched belongs to actions/cache/restore, not to actions/cache.
          if [ -d "$D" ] && [ -n "$(ls -A "$D" 2>/dev/null)" ]; then
            echo "CCACHE_RESTORED=1" >> $GITHUB_ENV
          fi
          # $GITHUB_ENV reaches the next step, not this one, so the directory has
          # to be named again here or the counters zeroed are the default ones.
          CCACHE_DIR="$D" ccache --zero-stats

      - name: Build
        run: |
          export CC=/usr/bin/gcc-10 CXX=/usr/bin/g++-10
          cmake -S . -B build -G Ninja -D CMAKE_BUILD_TYPE=RelWithDebInfo -D LITERT_VER="${LITERT_VER}" -D TFLITE_VER="${TFLITE_VER}" -D TFLITE_PLUGINS_COREML=OFF -D CMAKE_C_COMPILER_LAUNCHER=ccache -D CMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build -j$(nproc)

      - name: Verify
        run: |
          cc -o build/load_plugin test/load_plugin.c -ldl
          for lib in build/lib/*.so; do
            ./scripts/verify_plugin.sh "$lib"
            ./build/load_plugin "$lib" --reject-unknown-options
          done

      - name: Package
        run: |
          mkdir -p dist/lib debug
          cp build/lib/*.so dist/lib/
          # Split the debug info out, strip the .so, and leave a debuglink so a
          # debugger can find the separated file by name later.
          for lib in dist/lib/*.so; do
            base=$(basename "$lib")
            objcopy --only-keep-debug "$lib" "debug/$base.debug"
            strip --strip-debug --strip-unneeded "$lib"
            objcopy --add-gnu-debuglink="debug/$base.debug" "$lib"
          done
          ls -l dist/lib debug

          PKG="tflite_delegate_plugins-${{ matrix.triple }}.tar.gz"
          DBG="tflite_delegate_plugins-${{ matrix.triple }}-debug.tar.gz"
          tar -C dist -czf "$PKG" lib
          tar -C debug -czf "$DBG" .
          sha256sum "$PKG" | tee "$PKG.sha256"
          sha256sum "$DBG" | tee "$DBG.sha256"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.triple }}
          path: |
            *.tar.gz
            *.tar.gz.sha256

      - name: ccache statistics
        if: always()
        run: |
          ccache --show-stats | head -20
          # A restored cache with no hits means the path or the key is wrong, and
          # the only symptom is a job quietly as slow as a cold one.
          if [ -n "${CCACHE_RESTORED:-}" ]; then
            # ccache 4 prints "Hits:". ccache 3, which is what ubuntu:20.04
            # carries, prints "cache hit (direct)" and "(preprocessed)".
            # Reading only the former counts every ccache 3 run as zero hits and
            # fails a job whose cache was working fine.
            stats=$(ccache --show-stats)
            hits=$(printf '%s' "$stats" | awk '/^  Hits:/{print $2; exit}')
            if [ -z "$hits" ]; then
              direct=$(printf '%s' "$stats" | awk '/^cache hit \(direct\)/{print $NF; exit}')
              prep=$(printf '%s' "$stats" | awk '/^cache hit \(preprocessed\)/{print $NF; exit}')
              hits=$(( ${direct:-0} + ${prep:-0} ))
            fi
            if [ "${hits:-0}" -eq 0 ]; then
              echo "::error::a ccache cache was restored but nothing hit; check CCACHE_DIR against the cached path"
              exit 1
            fi
          fi
